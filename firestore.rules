rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tickets/{ticketId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /chatConversations/{conversationId}/{document=**} {
      allow read, write: if true;
    }
  }
}
